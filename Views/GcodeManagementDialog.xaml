<Window x:Class="PrintVault3D.Views.GcodeManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:PrintVault3D.Converters"
        mc:Ignorable="d"
        Title="Manage G-codes" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="CanResize"
        Background="{StaticResource BackgroundDarkBrush}">

    <Window.Resources>
        <converters:BytesToHumanReadableConverter x:Key="BytesToSize"/>
        <converters:TicksToTimeConverter x:Key="TicksToTime"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="G-code Management" 
                       Style="{StaticResource HeaderText}"
                       FontSize="22"/>
            <TextBlock x:Name="StatsText"
                       Style="{StaticResource CaptionText}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Filter Tabs + Search -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Filter Buttons -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <RadioButton x:Name="FilterAll" Content="All" 
                             GroupName="GcodeFilter" IsChecked="True"
                             Checked="Filter_Changed"
                             Style="{StaticResource {x:Type ToggleButton}}"
                             Padding="12,6" Margin="0,0,8,0"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             Background="Transparent"/>
                <RadioButton x:Name="FilterLinked" Content="Linked" 
                             GroupName="GcodeFilter"
                             Checked="Filter_Changed"
                             Style="{StaticResource {x:Type ToggleButton}}"
                             Padding="12,6" Margin="0,0,8,0"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             Background="Transparent"/>
                <RadioButton x:Name="FilterUnlinked" Content="Unlinked" 
                             GroupName="GcodeFilter"
                             Checked="Filter_Changed"
                             Style="{StaticResource {x:Type ToggleButton}}"
                             Padding="12,6"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             Background="Transparent"/>
            </StackPanel>

            <!-- Search Box -->
            <TextBox Grid.Column="1" 
                     x:Name="SearchBox"
                     Style="{StaticResource SearchTextBox}"
                     TextChanged="SearchBox_TextChanged"
                     Margin="16,0,0,0"/>
        </Grid>

        <!-- G-code List -->
        <Border Grid.Row="2" 
                Background="{StaticResource BackgroundMediumBrush}"
                CornerRadius="8"
                Padding="8">
            <ListView x:Name="GcodeListView"
                      Background="Transparent"
                      BorderThickness="0"
                      SelectionMode="Extended"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="Bd" 
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource BackgroundHoverBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource BackgroundSelectedBrush}"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentCyanBrush}"/>
                                            <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>

                            <!-- File Name -->
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding OriginalFileName}" 
                                           FontWeight="Medium"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding SlicerName}" 
                                           Style="{StaticResource CaptionText}"
                                           Opacity="0.7"/>
                            </StackPanel>

                            <!-- Print Time -->
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding PrintTimeFormatted}"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>

                            <!-- Filament -->
                            <TextBlock Grid.Column="2" 
                                       Text="{Binding FilamentWeight, StringFormat='{}{0:0.#}g'}"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>

                            <!-- Linked Model -->
                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                <Ellipse Width="8" Height="8" Margin="0,0,6,0">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="{StaticResource AccentRedBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ModelId, Converter={StaticResource BoolToVis}}" Value="Visible">
                                                    <Setter Property="Fill" Value="{StaticResource AccentGreenBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock Text="{Binding Model.Name, FallbackValue='Not linked'}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           TextTrimming="CharacterEllipsis"
                                           MaxWidth="120"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Actions Footer -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="LinkButton" 
                        Style="{StaticResource PrimaryButton}"
                        Content="Link to Model"
                        Click="LinkToModel_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="UnlinkButton" 
                        Style="{StaticResource SecondaryButton}"
                        Content="Unlink"
                        Click="Unlink_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="DeleteButton" 
                        Style="{StaticResource DangerButton}"
                        Content="Delete"
                        Click="Delete_Click"/>
            </StackPanel>
            <Button Style="{StaticResource SecondaryButton}"
                    Content="Close"
                    Click="Close_Click"
                    HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</Window>

